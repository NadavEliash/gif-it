'use client'

import { useCallback, useEffect, useRef, useState } from "react"
import { Fredoka } from "next/font/google"

const fredoka = Fredoka({ subsets: ['latin'] })

export default function Filters() {
    
    return (
        <>
            <div>Filters works</div>
        </>
    )
}